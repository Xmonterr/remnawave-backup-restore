# Быстрая диагностика проблем с Яндекс.Диском

## Симптомы
Скрипт зависает на этапе:
```
[INFO] Загрузка файла на Яндекс.Диск...
```

## Быстрые проверки

### 1. Доступность API (5 секунд)
```bash
curl -I https://cloud-api.yandex.net
```
**Ожидаемый результат:** HTTP/2 200 или 301/302  
**Если timeout:** Яндекс.Диск заблокирован или проблемы с интернетом

### 2. Проверка токена (10 секунд)
```bash
# Замените YOUR_TOKEN на ваш токен
curl -s -H "Authorization: OAuth YOUR_TOKEN" \
  https://cloud-api.yandex.net/v1/disk/ | jq .
```
**Ожидаемый результат:** Информация о диске (total_space, used_space)  
**Если ошибка:** Токен невалиден или истек

### 3. Тест скорости (30 секунд)
```bash
# Создайте тестовый файл
echo "test" > /tmp/test.txt

# Получите ссылку для загрузки
UPLOAD_URL=$(curl -s -H "Authorization: OAuth YOUR_TOKEN" \
  "https://cloud-api.yandex.net/v1/disk/resources/upload?path=test.txt&overwrite=true" | jq -r .href)

# Загрузите с отображением времени
time curl --progress-bar -X PUT "$UPLOAD_URL" \
  -H "Authorization: OAuth YOUR_TOKEN" \
  --data-binary "@/tmp/test.txt"
```
**Анализ:**
- < 2 секунды: отличная скорость
- 2-10 секунд: нормальная скорость
- > 10 секунд: медленное соединение, большие файлы будут загружаться очень долго

### 4. Автоматическая диагностика
```bash
# Скачайте и запустите скрипт диагностики
wget https://raw.githubusercontent.com/distillium/remnawave-backup-restore/main/yandex-disk-troubleshooting.sh
chmod +x yandex-disk-troubleshooting.sh
./yandex-disk-troubleshooting.sh YOUR_TOKEN
```

## Распространенные причины зависания

### 🐌 Низкая скорость интернета
**Признаки:**
- Тест скорости показывает > 10 секунд для маленького файла
- Файл бэкапа большой (> 100 MB)

**Решение:**
- Дождитесь завершения (может занять 30-60+ минут для больших файлов)
- Используйте другой метод загрузки (Telegram, Google Drive)
- Загружайте в время меньшей нагрузки на сеть

### 🚫 Блокировка ресурсов Яндекса
**Признаки:**
- `curl -I https://cloud-api.yandex.net` возвращает timeout
- Другие российские сервисы также недоступны

**Решение:**
- Используйте VPN
- Проверьте с другого провайдера/сети
- Переключитесь на другой метод загрузки

### ⏱️ Timeout в старой версии скрипта
**Признаки:**
- Скрипт висит бесконечно без прогресса
- Версия скрипта < 2.1.0

**Решение:**
- Обновите скрипт до последней версии
- Новая версия имеет:
  - Таймауты соединения (30 сек)
  - Максимальное время загрузки (1 час)
  - Прогресс-бар загрузки
  - Проверку доступности API

### 💾 Недостаточно места на диске
**Признаки:**
- Получена ссылка для загрузки, но загрузка не завершается
- Ошибка "DiskSpaceLimitExceeded"

**Решение:**
```bash
# Проверьте свободное место
curl -s -H "Authorization: OAuth YOUR_TOKEN" \
  https://cloud-api.yandex.net/v1/disk/ | jq '.total_space, .used_space'
```
- Освободите место на Яндекс.Диске
- Удалите старые бэкапы вручную

### 🔑 Проблемы с токеном
**Признаки:**
- Ошибка при получении ссылки для загрузки
- "Unauthorized" или "Invalid token"

**Решение:**
1. Получите новый токен: https://yandex.ru/dev/disk/poligon
2. Обновите токен в скрипте:
```bash
rw-backup
# Выберите: 6. Настройки конфигурации → 3. Настройки Яндекс.Диск
```

## Что изменилось в новой версии (v2.1.0+)

### До (проблемная версия):
```bash
curl -s -X PUT "$upload_href" --data-binary "@$file_path"
# ❌ Нет таймаутов - может висеть бесконечно
# ❌ Silent mode - не видно прогресса
# ❌ Нет проверки доступности
```

### После (улучшенная версия):
```bash
# ✅ Проверка доступности перед началом
curl --connect-timeout 10 https://cloud-api.yandex.net

# ✅ Загрузка с прогрессом и таймаутами
curl --progress-bar --connect-timeout 30 --max-time 3600 \
  -X PUT "$upload_href" --data-binary "@$file_path"
```

### Добавлено:
- ✅ Проверка доступности API перед загрузкой
- ✅ Таймаут соединения: 30 секунд
- ✅ Максимальное время загрузки: 1 час
- ✅ Прогресс-бар загрузки (видно, что процесс идет)
- ✅ Подробные сообщения об ошибках
- ✅ Показ размера файла перед загрузкой
- ✅ Коды ошибок curl для диагностики

## Рекомендации

### Для больших бэкапов (> 500 MB):
1. Используйте Telegram (лимит 2 GB)
2. Или Google Drive (без ограничений)
3. Или загружайте на Яндекс.Диск в периоды низкой загрузки сети

### Для надежности:
1. Настройте несколько методов загрузки
2. Проверяйте логи регулярно
3. Тестируйте восстановление из бэкапов

### Мониторинг:
```bash
# Следите за процессом загрузки в реальном времени
tail -f /var/log/rw-backup.log  # если логирование настроено
```

## Дополнительная помощь

Если проблема не решается:
1. Запустите полную диагностику: `./yandex-disk-troubleshooting.sh`
2. Проверьте версию скрипта: `rw-backup` → смотрите версию в заголовке
3. Обновите скрипт: `rw-backup` → выберите "Обновить скрипт"
4. Создайте issue: https://github.com/distillium/remnawave-backup-restore/issues
